apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: loki-data
  namespace: starknet
  labels:
    app: loki
    component: storage
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 20Gi
