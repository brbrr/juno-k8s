apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: grafana-data
  namespace: starknet
  labels:
    app: grafana
    component: storage
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 2Gi
