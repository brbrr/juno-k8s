apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: loki-data
  namespace: starknet
  labels:
    app: loki
    component: storage
    environment: dev
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 2Gi  # Reduced from 20Gi for testing
