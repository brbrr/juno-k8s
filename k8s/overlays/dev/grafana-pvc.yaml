apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: grafana-data
  namespace: starknet
  labels:
    app: grafana
    component: storage
    environment: dev
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi  # Reduced from 2Gi for testing
